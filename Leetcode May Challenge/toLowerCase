class Solution {
public:
    string toLowerCase(string s) {
        
        for(int i=0;i<s.size();i++){
            int temp = s[i];
            if(temp>=65 && temp<=90){
                s[i] = temp + 32; 
            }
        }
        return s;
        
    }
};
