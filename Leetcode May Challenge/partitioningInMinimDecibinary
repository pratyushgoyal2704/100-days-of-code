class Solution {
public:
    int minPartitions(string n) {
        int ans = -1;
        for(int i=0;i<n.size();i++){
            int temp = n[i] - '0';
            ans = max(ans,temp);
        }
        return ans;
    }
};
